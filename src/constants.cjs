const MESSAGE_TYPE = ['text', 'file', 'media'];
const FRIENDSHIP_STATUS = ['pending', 'accepted', 'rejected', 'blocked', 'cancelled'];
const NOTIFICATION_TYPE = ['friend_request_accepted', 'friend_request_rejected'];

module.exports = {
    MESSAGE_TYPE,
    FRIENDSHIP_STATUS,
    NOTIFICATION_TYPE
};